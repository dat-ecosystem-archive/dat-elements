{
  "name": "dat-elements",
  "version": "1.0.0",
  "description": "Dat UI elements",
  "main": "index.js",
  "scripts": {
    "start": "bankai start --entry=example/index.js --js.transform=sheetify/transform --port 8088",
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "datproject/dat-elements",
  "keywords": [
    "elements",
    "dat",
    "ui"
  ],
  "license": "MIT",
  "dependencies": {
    "base-elements": "^2.0.0",
    "bel": "^4.5.0",
    "sheetify": "^5.1.1",
    "xtend": "^4.0.1"
  },
  "devDependencies": {
    "bankai": "^3.3.1",
    "choo": "^4.0.0-2",
    "dependency-check": "^2.6.0",
    "istanbul": "^0.4.5",
    "tachyons": "^4.5.4",
    "tape": "^4.6.2"
  }
}
